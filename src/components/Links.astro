---
let links = ['home','gallery','post','about']
const {classes} = Astro.props
---

{
  links.map((link) => (
    <li class="flex">
      <a
        class={`capitalize ${classes}`}
        href={link === 'home' ? '/' : `/${link}`}
      >
        {link}
      </a>
    </li>
  ))
}
